server:
  port: ${server.port}
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: ${discovery.server-addr}
      config:
        server-addr: localhost:8848
        file-extension: yaml
        group: TEST_GROUP
